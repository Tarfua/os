[package]
name = "boot"
version = "0.1.0"
edition = "2021"
description = "Builds bootable disk image from kernel (Stage 1.1)"

[build-dependencies]
bootloader = { version = "0.11", default-features = false, features = ["bios"] }
